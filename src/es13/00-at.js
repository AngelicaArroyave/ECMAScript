// Obtiene la última posición del arreglo
const numbers = ['One', 'Two', 'Three', 'Four', 'Five', 'Six', 'Seven']
console.log(numbers[numbers.length - 1]); // Seven
console.log(numbers.at(-1)); // Seven