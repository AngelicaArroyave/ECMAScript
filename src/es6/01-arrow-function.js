// Función tradicional
function square(num) {
    return num * num
}

// Función de flecha
const square = (num) => {
    return num * num
}

// Función de flecha con retorno implícito
const square = num => num * num

// Función de flecha con retorno implícito
const square = (num1, num2) => (
    num1 * num2
)